services:
  app:
    build: .
    volumes:
      - .:/app                # Sync local folder to container
      - /app/node_modules     # Anonymous volume to protect container's modules
    ports:
      - "3000:3000"
    command: npm start
    environment:
      - NODE_ENV=development


